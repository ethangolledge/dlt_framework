interface:
  display_name: "Build dlt Source"
  short_description: "Build production-ready client dlt sources"
  default_prompt: "Use $build-dlt-source to implement and validate a production-ready client extraction source."
